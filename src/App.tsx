import { AppPagination } from "./components/pagination";

function App() {
  return (
    <div style={{
      margin: "0 30%",
      display: "flex",
      flexDirection: "column"
    }}>
      <AppPagination />
    </div>
  );
}

export default App
